print("+".join("asdf")) # 문자열 사이에 +를 추가한다.
print("+".join(['a','b','c','d']))  # 문자열 사이에 +를 추가한다.
print(['a','b','c','d'])    
print("-"*15)

a = "wowowowowowowowow:OSIFEJdf"
print(a.upper())    # 문자열을 전부 대문자로 만든다.
print(a.lower())    # 문자열을 전부 소문자로 만든다.
